#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H
/*
 * Header file that defines a macro ABS(x) that
 * computes the absolute value of a number
 */
#define ABS(x) (((x) >= 0) ?  ((x) * 1) : ((x) * -1))
#endif
